import React from 'react';
import { BlockType } from '@blink-mind/core';
// import { BraftDescEditor } from './braft-desc-editor';

export function BraftDescEditorPlugin() {
  return {
    // renderTopicDescEditor(props) {
    //   return <BraftDescEditor {...props} />;
    // },
    //
    // serializeBlockData(props, next) {
    //   const { block } = props;
    //   if (block.type === BlockType.DESC) {
    //     if (block.data.kind === 'html') {
    //       return typeof block.data.data === 'string'
    //         ? block.data.data
    //         : block.data.data.toHTML();
    //     }
    //   }
    //   return next();
    // }
  };
}
